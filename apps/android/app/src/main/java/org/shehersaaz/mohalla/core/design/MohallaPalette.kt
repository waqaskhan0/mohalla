package org.shehersaaz.mohalla.core.design

import androidx.compose.ui.graphics.Color

/**
 * GENERATED from docs/prototype.html (approved Stage 3) by
 * packages/design-tokens/scripts/generate.mjs. DO NOT EDIT BY HAND - rerun
 * `npm run generate --workspace @mohalla/design-tokens`.
 *
 * 24 colour tokens, each with an approved value behind it. A colour that is
 * not here has no approved value, and inventing one is a defect (UI/UX §16).
 *
 * NO DARK VARIANTS. Dark mode is explicitly out of V1 scope, and the approved
 * prototype declares no dark values - so there is nothing to generate and
 * nothing to guess.
 */
object MohallaPalette {
    /** `--bg-primary` */
    val BgPrimary = Color(0xFFF6F4EF)

    /** `--bg-secondary` */
    val BgSecondary = Color(0xFFEFECE5)

    /** `--surface-primary` */
    val SurfacePrimary = Color(0xFFFFFFFF)

    /** `--surface-sunken` */
    val SurfaceSunken = Color(0xFFF1EEE7)

    /** `--text-primary` */
    val TextPrimary = Color(0xFF1C2530)

    /** `--text-secondary` */
    val TextSecondary = Color(0xFF566573)

    /** `--text-tertiary` */
    val TextTertiary = Color(0xFF8B98A5)

    /** `--text-inverse` */
    val TextInverse = Color(0xFFFFFFFF)

    /** `--border-default` */
    val BorderDefault = Color(0xFFE2DED5)

    /** `--border-strong` */
    val BorderStrong = Color(0xFFCFC9BD)

    /** `--brand-primary` */
    val BrandPrimary = Color(0xFF145A73)

    /** `--brand-primary-hover` */
    val BrandPrimaryHover = Color(0xFF0F485C)

    /** `--brand-primary-subtle` */
    val BrandPrimarySubtle = Color(0xFFE4EFF4)

    /** `--brand-secondary` */
    val BrandSecondary = Color(0xFF6B4E9E)

    /** `--brand-secondary-subtle` */
    val BrandSecondarySubtle = Color(0xFFEFEAF7)

    /** `--success` */
    val Success = Color(0xFF1F7A5C)

    /** `--success-subtle` */
    val SuccessSubtle = Color(0xFFE3F1EC)

    /** `--warning` */
    val Warning = Color(0xFF9A6510)

    /** `--warning-subtle` */
    val WarningSubtle = Color(0xFFFBF0DE)

    /** `--error` */
    val Error = Color(0xFFA32C2C)

    /** `--error-subtle` */
    val ErrorSubtle = Color(0xFFFAE7E5)

    /** `--shell` */
    val Shell = Color(0xFF20303C)

    /** `--shell-2` */
    val Shell2 = Color(0xFF2A3D4B)

    /** `--shell-t` */
    val ShellT = Color(0xFFC6D4DE)
}
